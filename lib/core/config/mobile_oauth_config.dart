/// Deep link registered in Supabase Auth → URL Configuration → Redirect URLs.
/// Must match the intent filter on Android and CFBundleURLSchemes on iOS.
const mobileGoogleOAuthRedirectUrl = 'com.example.hatchlog_m://login-callback/';
